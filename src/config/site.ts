export const siteConfig = {
  name: 'Uniflex Global Logistics',
  description:
    'Truck dispatch for owner-operators and small fleets — we book the loads, negotiate the rates, and handle the paperwork.',
  // Set NEXT_PUBLIC_APP_URL in .env.local / .env to your real domain before deploying.
  url: process.env.NEXT_PUBLIC_APP_URL ?? 'https://uniflexlogistics.com',
  ogImage: '/opengraph-image',
}
